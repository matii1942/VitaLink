# Make the engines field in package.json binding rather than advisory.
#
# Without this, npm ignores it completely — not even a warning — and a
# contributor on a different Node or npm installs happily and hits the
# differences later, in ways that look like bugs in the code. With it, npm
# refuses and says exactly which version is required and which is present.
engine-strict=true
